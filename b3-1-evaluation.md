# B3-1 평가 문항

> **미션:** 정보를 엄청 빠르게 찾아주는 작은 저장소 만들기  
> **분야:** 자료구조와 알고리즘  
> **평가 범위:** 기능 동작, 기본 자료구조 구현, LRU·TTL·메모리 관리, 확장성과 구조 개선

각 문항을 확인한 뒤 충족한 항목에 체크합니다.

---

## 항목 1. 기능 동작 확인

- [ ] String 타입 기본 동작과 관련하여 `SET`, `GET`, `DEL`, `EXISTS`, `DBSIZE`, `KEYS` 명령어가 모두 정상 동작하는가?
- [ ] `maxmemory` 설정 후 메모리 제한을 초과했을 때 LRU 정책에 따라 가장 오래 사용되지 않은 키가 자동으로 제거되는가?
- [ ] `INFO memory` 명령어 실행 시 `used_memory`, `maxmemory`, `evicted_keys`가 규칙에 맞게 출력되는가?
- [ ] TTL 관리와 관련하여 `EXPIRE`, `TTL` 규칙이 정상 동작하며 만료된 키가 적절히 제거되는가?
- [ ] 에러 처리와 관련하여 잘못된 명령, 잘못된 인자, 정수 파싱 오류, OOM 상황이 표준 형식으로 출력되는가?

---

## 항목 2. 기본 자료구조 구현 설명

- [ ] 이중 연결 리스트의 노드 구조인 `prev`, `next`, `data` 필드와 핵심 메서드들이 O(1)로 동작하도록 어떻게 구성했는지 설명할 수 있는가?
- [ ] 해시맵에서 직접 설계한 해시 함수가 어떤 입력을 받아 어떤 과정을 거쳐 인덱스를 만드는지 설명할 수 있는가?
- [ ] 충돌 해결을 체이닝 방식으로 어떻게 구현했는지 설명할 수 있는가? 이때 버킷 내부 구조를 어떤 방식으로 선택했는지도 함께 설명할 수 있는가?
- [ ] 로드 팩터가 `0.75`를 초과했을 때 버킷을 2배로 확장하는 절차를 설명할 수 있는가?

---

## 항목 3. LRU, TTL, 메모리 관리 흐름 설명

- [ ] LRU 구현에서 해시맵과 이중 연결 리스트가 각각 어떤 역할을 하며, 왜 두 자료구조가 모두 필요한지 설명할 수 있는가?
- [ ] O(1) LRU 달성 원리를 “조회는 해시맵으로 처리하고, 사용 기록 갱신은 리스트 이동으로 처리한다”는 관점에서 설명할 수 있는가?
- [ ] TTL 관리에 힙을 사용한 이유를 설명할 수 있는가? 특히 힙이 가장 빠른 만료 시간을 빠르게 찾는 성질을 가진다는 점을 설명할 수 있는가?
- [ ] 메모리 초과 시 eviction 흐름을 단계별로 설명할 수 있는가? 이때 `used_memory` 산정 및 갱신, LRU 제거, `evicted_keys` 증가까지 포함하여 설명할 수 있는가?
- [ ] `GET` 명령어의 전체 흐름을 순서대로 설명할 수 있는가? 즉, TTL 확인, 만료 여부 판단, 만료된 키 삭제, 값 반환, LRU 갱신 조건을 순서대로 설명할 수 있는가?

---

## 항목 4. 확장 사고 및 구조 개선 설명

- [ ] “LRU 대신 LFU 정책을 구현한다면 자료구조를 어떻게 변경해야 하는가?”라는 질문에 합리적으로 답변할 수 있는가?
- [ ] 데이터가 10만 건으로 늘어나면 현재 구조에서 병목이 될 수 있는 부분은 어디이며, 어떻게 개선할지 설명할 수 있는가?
- [ ] `used_memory`에 자료구조 오버헤드까지 포함하는 모델로 변경할 경우 무엇이 달라지는지 설명할 수 있는가?
- [ ] 자료구조 오버헤드를 포함하는 모델을 사용할 때 공정한 비교 또는 채점을 위해 어떤 보정이 필요한지 설명할 수 있는가?

---

## 평가 결과 기록

| 평가 항목 | 충족 | 부분 충족 | 미충족 | 비고 |
|---|:---:|:---:|:---:|---|
| 항목 1. 기능 동작 확인 | [ ] | [ ] | [ ] | |
| 항목 2. 기본 자료구조 구현 설명 | [ ] | [ ] | [ ] | |
| 항목 3. LRU, TTL, 메모리 관리 흐름 설명 | [ ] | [ ] | [ ] | |
| 항목 4. 확장 사고 및 구조 개선 설명 | [ ] | [ ] | [ ] | |

---

## 최종 확인

- [ ] 모든 기본 명령어, TTL, 메모리 제한, LRU 제거 및 오류 상황을 실행 결과 또는 테스트로 증빙할 수 있다.
- [ ] 이중 연결 리스트, 해시맵, 체이닝, 리사이징, 최소 힙의 구현 원리를 실제 코드를 근거로 설명할 수 있다.
- [ ] `GET`, TTL 만료, 메모리 초과 및 eviction의 전체 처리 흐름을 순서대로 설명할 수 있다.
- [ ] LFU 전환, 10만 건 규모의 병목, 메모리 오버헤드 포함 모델과 채점 보정 방안을 논리적으로 제안할 수 있다.
